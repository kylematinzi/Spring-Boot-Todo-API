package com.testproject.spring_boot_test_project.model.enums;

/**
 * This creates and enum called Role. It gives a defined list of options. This will
 * ensure an incorrect role or misspelling is not introduced.
 */
public enum Role {
    USER,
    ADMIN
}